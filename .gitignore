/target

# GENESIS binary (external) and simulation run artifacts
/atdyn
*.dcd
*.log
*.rst
*.pdb
*.vmd
*.gro
*.crd
*.itp
*.psf
*.top
*.gif

# GENESIS control files (generated by `--inp`) and run scripts
*.inp
*.sh
# ...but the curl/wget installer is source, not generated output.
!/install.sh

# Bundled force-field parameter tables are source data, not run output — keep them
!/tests/fixtures/isolf.itp
